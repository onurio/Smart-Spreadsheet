version: "3.8"

services:
  api:
    build:
      context: ./api
    container_name: excel-ai-api
    ports:
      - "8000:8000"
  ui:
    build:
      context: ./ui
    container_name: excel-ai-ui
    ports:
      - "8001:8001"
